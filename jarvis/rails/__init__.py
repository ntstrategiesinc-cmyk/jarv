"""The rails — the safety posture applied across the harness.

A single confirmation gate between tool-choice and tool-run, a plain audit log with a model-cost
tally, prompt-injection defense (external content is data, not commands), and a kill switch that
pauses proactive behavior. Voice and heartbeat inherit all of it because every path funnels
through the same agent core.
"""
