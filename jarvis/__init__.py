"""Jarvis — a voice-first personal AI assistant harness.

One shared agent core (jarvis.core), many adapters in and out (jarvis.adapters,
jarvis.heartbeat). See AGENT.md at the repo root for the spec.
"""

__version__ = "0.1.0"
