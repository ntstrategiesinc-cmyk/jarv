"""Long-term memory — durable facts that survive restarts.

Distinct from the in-session conversation history (short-term). Plain and human-readable so the
owner can open, correct, or delete it at any time.
"""
